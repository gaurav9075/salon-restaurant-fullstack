# Server
server.port=8080

# ─── MySQL ───────────────────────────────────────
spring.datasource.url=jdbc:mysql://localhost:3306/booking_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# ─── Email (Gmail SMTP — free) ────────────────────
# Replace with your Gmail + App Password
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=gauravshinde8182@gmail.com
spring.mail.password=iwcl hopp ccwp cmyr
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# App Config
app.mail.from=your_email@gmail.com
app.business.salon.email=hello@glowstudio.com
app.business.restaurant.email=hello@thecornertable.com.au

# CORS — allow both React dev servers
app.cors.allowed-origins=http://localhost:3000,http://localhost:3001

# Logging
logging.level.com.bookingapp=DEBUG
